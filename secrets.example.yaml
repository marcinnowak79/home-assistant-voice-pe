wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
gemini_proxy_url: "ws://homeassistant.local:8765"

# Only needed when flashing from the CLI (not via the Home Assistant ESPHome add-on).
# Copy your device's API encryption key from Home Assistant and uncomment, then set
# `api: encryption: key: !secret api_encryption_key` in the yaml. See README -> Troubleshooting.
# api_encryption_key: "YOUR_DEVICE_API_ENCRYPTION_KEY"
